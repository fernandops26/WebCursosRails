<div class="container" style="margin-top:1em;">
  <div class="row">
    
    <div class="col-md-3">  
      <h1>Lista de Coordinadores</h1>
      <p>Aquí encontras la lissa de coordinadores que agregas manualmente como los que envian su currculum</p>
      <%= link_to 'Nuevo Coordinador', new_coordinator_path,{class:'btn btn-success'} %>

    </div>
    <div class="col-md-9" style="padding-top:1em;">
    <p id="notice"><%= notice %></p>
      <table class="table table-hover">
        <thead>
          <tr>
            <th></th>
            <th>Nombres</th>
            <th>Apellidos</th>
            <th>Curriculum</th>
            <th>Estado</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @coordinators.each do |coordinator| %>
            <tr>
              <td>
                <%if coordinator.visto==false%>
                <label class="label label-success">Nuevo</label>
              <%end%>
              </td>
              <td><%= coordinator.nombres %></td>
              <td><%= coordinator.apellidos %></td>
              <td><%if coordinator.curriculum %>

                 <%=link_to 'Ver curriculum' ,coordinator.curriculum_url ,{:target=>"_blank"}%>
                  <%else%>
                  No disponible
                  <%end%>
                </td>
              <td><% if coordinator.estado==true %>
                  Activo
                <%else%>
                  Inactivo
                <%end%>
              </td>
              <td><%= link_to 'Ver', coordinator %></td>
              <td><%= link_to 'Editar', edit_coordinator_path(coordinator) %></td>
              <td><%= link_to 'Eliminar', coordinator, method: :delete, data: { confirm: '¿Estas seguro?' } %></td>
            </tr>
          <% end %>
        </tbody>
      </table>

    </div>
  </div>
</div>




<br>
