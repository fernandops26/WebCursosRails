<div class="container" style="margin-top:2.5em">
	<div class="row">
		<div class="col-md-5 text-center col-md-offset-3" >
			<% if notice %>
				<div class="alert alert-success">
				<a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
						<strong id="notice"><%= notice %></strong>
					</div>
			<% end %>
					

					<% if flash[:error] %>
				    <div id="error_explanation" class="alert alert-danger">
				      <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
				      <strong><%=flash[:error]%></strong>
				    </div>
				  <% end %>
			<h3>Gestiona tu contraseña</h3>

			<%= form_for @user,url:admin_profile_path  do |u|%>
			<div class="form-group">
				<%=u.hidden_field :username%>
			<%=u.password_field :password,{placeholder:'Nueva contraseña',class:'form-control'}%>
			</div>
			<div class="form-group">
			
				<%=u.password_field :password_digest,{placeholder:'Confirma ueva contraseña',class:'form-control'} %>
			</div>
				
			<%=u.submit 'Cambiar contraseña' ,{class:'btn btn-info'}%>
			<%end%>
		</div>
	</div>
</div>